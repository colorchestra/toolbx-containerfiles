FROM registry.fedoraproject.org/fedora-toolbox:42

ARG NAME=homebox

LABEL name="$NAME" \
      summary="Fedora toolbox base container" \
      maintainer="morph"

ENV EDITOR=nvim

# Install packages
RUN dnf -y upgrade \
  && dnf -y install \
  tmux \
  htop \
  hugo \
  python3-pip \
  ripgrep \
  neovim \
  ncdu \
  golang \
  && dnf clean all

#RUN pip install \
#  yt-dlp
